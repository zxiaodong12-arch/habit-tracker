# 🚀 快速推送代码到 GitHub

由于 SSH 配置可能比较复杂，这里提供最简单的 **Personal Access Token** 方式。

## ⚡ 3 步完成（约 2 分钟）

### 步骤 1：创建 Personal Access Token

1. **打开创建页面**
   - 直接访问：https://github.com/settings/tokens/new
   - 或者：GitHub → 右上角头像 → Settings → Developer settings → Personal access tokens → Tokens (classic) → Generate new token (classic)

2. **填写信息**
   - **Note**（备注）：`habit-tracker-deploy`
   - **Expiration**（过期时间）：选择 `90 days` 或 `No expiration`
   - **Select scopes**（权限）：**只勾选 `repo`**（这会自动勾选所有 repo 相关权限）

3. **生成并复制 Token**
   - 点击页面底部的 **"Generate token"**（生成令牌）
   - **重要**：立即复制显示的 token（类似 `ghp_xxxxxxxxxxxxxxxxxxxx`）
   - ⚠️ **只显示一次，关闭页面后就看不到了！**

### 步骤 2：推送代码

在终端运行：

```bash
cd /System/Volumes/Data/data/RD/habit-tracker
git push -u origin main
```

当提示输入时：
- **Username**: `zxiaodong12@gmail.com`
- **Password**: **粘贴你的 token**（不是 GitHub 密码！直接粘贴刚才复制的 token）

### 步骤 3：启用 GitHub Pages

推送成功后：

1. 进入仓库：https://github.com/zxiaodong12-arch/habit-tracker
2. 点击 **Settings**（设置）
3. 左侧菜单找到 **Pages**（页面）
4. 在 **Source**（源）部分：
   - 选择 **GitHub Actions**
   - 或者选择 **Deploy from a branch** → 选择 `gh-pages` 分支 → `/ (root)` 目录
5. 点击 **Save**（保存）

## ✅ 完成！

等待几分钟，GitHub Actions 会自动构建并部署你的网站。

访问地址：
- `https://zxiaodong12-arch.github.io/habit-tracker`

## 📝 注意事项

1. **Token 安全**：
   - Token 相当于密码，不要分享给他人
   - 如果泄露，立即在 https://github.com/settings/tokens 删除并重新生成

2. **Token 过期**：
   - 如果设置了过期时间，到期后需要重新生成
   - 建议设置为较长时间或 No expiration

3. **下次推送**：
   - macOS 的 Keychain 可能会记住 token
   - 如果提示输入密码，再次粘贴 token 即可

## 🔄 如果推送失败

如果还是提示认证失败：

1. **检查 token 是否正确复制**（包括开头的 `ghp_`）
2. **确认勾选了 `repo` 权限**
3. **尝试重新生成 token**

## 🎯 下一步

部署完成后，记得：
1. 配置 API 地址（如果需要）：在 GitHub Secrets 中添加 `VITE_API_BASE_URL`
2. 查看部署状态：进入仓库 → **Actions** 标签页
3. 查看详细文档：`部署到GitHub-Pages.md`
